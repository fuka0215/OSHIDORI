<h1>プロフィール編集</h1>

<%= form_with model: @user do |f| %>
  <label>ユーザ名</label>
  <%= f.text_field :name %>
  <label>自己紹介</label>
  <%= f.text_area :bio %>
  <label>プロフィール画像</label>
  <%= f.file_field :profile_image, accept: "image/*" %>
  <%= f.submit "変更を保存" %>
<% end %>
